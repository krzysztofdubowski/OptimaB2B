{% extends 'starter.html.twig' %}

 {% block MainContent %}
    <h1>Dokument creation</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <input type="submit" value="Create" />
    {{ form_end(form) }}

    <ul>
        <li>
            <a href="{{ path('doc_index') }}">Back to the list</a>
        </li>
    </ul>
{% endblock %}
